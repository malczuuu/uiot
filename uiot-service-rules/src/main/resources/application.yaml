server:
  port: 8334
spring:
  application:
    name: uiot-service-rules
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: uiot-service-rules
  kafka:
    producer:
      client-id: ${spring.application.name}
      bootstrap-servers: localhost:9092
      retries: 3
  jackson:
    default-property-inclusion: non_null
uiot:
  rules:
    kafka-system-events-topic: uiot-system-events
